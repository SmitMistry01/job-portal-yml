server:
  port: 8084
spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/admin_db'
    username: root
    password: smit
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:7779/eureka/'
