server:
  port: 8083

spring:
  application:
    name: notification-service

  rabbitmq:
    host: rabbitmq 
    port: 5672
    username: guest
    password: guest


  mail:
    host: smtp.gmail.com 
    port: 587
    username: jobportall121@gmail.com
    password: tyrwqxhcmvwaxjjq 
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:mysql://mysql-db:3306/notification_db?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: smit
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
